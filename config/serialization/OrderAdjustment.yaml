Siganushka\OrderBundle\Entity\AbstractOrderAdjustment:
    attributes:
        id:
            groups: [order.item, order.collection]
        amount:
            groups: [order.item, order.collection]
        type:
            groups: [order.item, order.collection]
        createdAt:
            groups: [order.item, order.collection]
